const app = require("./app");

// // Left for local testing
// const PORT = process.env.PORT || 5000;
// app.listen(PORT, () => {
//   console.log(`🔁 Server running on http://localhost:${PORT}`);
// });

// // Use serverless serverless function for deployment
module.exports = app;
